============
Installation
============

At the command line::

    $ easy_install django-editable

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-editable
    $ pip install django-editable